server:
  address: ":8080"

llm:
  provider: "openai"
  base_url: "https://api.openai.com"
  api_key: ""
  model: "gpt-4o-mini"
  timeout_seconds: 30

memory:
  backend: "redis" # redis | in_memory
  redis_addr: "redis:6379"
  redis_db: 0
  redis_password: ""
  key_prefix: "agent"
  history_size: 20
